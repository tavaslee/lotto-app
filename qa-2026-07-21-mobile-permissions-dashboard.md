# 2026-07-21 手勢、會員權限、開獎預設與流量儀表板 QA

## 實際頁面驗證

桌機首頁與 `/admin` 管理總覽均成功載入，沒有再出現 `GestureImageViewer` 或 `optionalMemberProcedure` 模組解析錯誤。管理總覽已顯示網站流量儀表板，包含 7／30／90 天、更新、PV、UV、人均瀏覽、手機占比、每日趨勢、熱門頁面、裝置分布與流量來源；因功能剛啟用，首張截圖為尚無歷史資料的正常空狀態。

390×844 手機首頁成功載入輪播與最新開獎區；輪播滑動、點擊放大與手勢縮放另由 React 指標事件測試覆蓋。版路圖片共用檢視器已移除「-」「+」「⟳」「×」工具列，只保留點擊背景返回頁面及雙指縮放／拖曳。

手機後台開獎維護顯示大樂透期數 `115000072` 與民國日期 `115.07.21` 的可編輯預設欄位。付費會員「李慧君」編輯對話框實際顯示大樂透、威力彩、今彩539、六合彩、加州天天樂五個可多選勾選欄位；本次只檢視後關閉，未儲存或改動會員資料。

## 自動化驗證進度

型別檢查已通過。重新命名後共有 24 個 Vitest 測試檔、106 項測試納入執行；管理總覽測試唯一的重複文字選取問題已修正並單檔重跑通過。完整回歸與正式建置將在發佈前再執行一次。

## 管理總覽瀏覽器文字證據

以已登入管理員工作階段實際開啟 `/admin`，頁面擷取文字明確包含「瀏覽量 PV」「訪客 UV」「人均瀏覽」「手機占比」「每日流量趨勢」「熱門頁面」「裝置分布」「流量來源」，並顯示「7 天」「30 天」「90 天」「更新」控制。功能剛上線尚未累積流量時，各區顯示「尚無資料」而非模擬數據。

已實際點擊「7 天」按鈕；頁面重新查詢後仍正常顯示完整指標區，並同時載入會員總數 3、付費會員 1、版路圖片 4 與 Google Sheets 正常狀態，無錯誤提示。

接續實際點擊「90 天」，再切換回「30 天」；兩次查詢後頁面均完整保留 PV、UV、每日趨勢、熱門頁面、裝置分布及流量來源區塊，沒有錯誤提示。回到 30 天後，「更新」按鈕已恢復為可操作狀態。

最後實際點擊「更新」並等待查詢完成；按鈕恢復可用，30 天儀表板、會員總數 3、付費會員 1、版路圖片 4 與 Sheets 正常狀態均完整保留，頁面沒有載入或查詢錯誤。
