# 首頁廣告輪播 QA 紀錄

## 2026-07-20 桌機驗證

- 首頁目前無上架輪播圖片，`首頁廣告輪播` 區塊未渲染；`最新開獎號碼` 緊接於主內容頂端，未留下輪播空白。
- 首頁側邊欄只顯示版路拖牌、走勢分析、連碰計算器及立柱計算器，已無「會員查詢規則」區塊與相關文案。
- 已登入管理員的「版路多圖管理」頁顯示 `首頁廣告輪播` 管理卡，包含前台顯示、自動換圖、0.5～10 秒間隔、儲存設定，以及 JPG／PNG／WebP／GIF／AVIF 多檔上傳區。
- 後台目前顯示 0 張輪播圖片，頁面結構與既有版路圖片管理區並存，無版面重疊。

## 驗證環境

開發預覽網址為 `https://3000-ivohd0lkw1if4tox6ce07-f364b6b4.sg1.manus.computer/`。管理員 `terry lee` 的已驗證工作階段可正常開啟 `/admin?section=images`，Google Sheets 健康狀態顯示已連線；輪播區仍為 0 張圖片，等待一次性 QA 圖片上傳。

## 有圖狀態驗證

以兩張不寫入資料庫、只在開發預覽暫時注入的 3:1 QA 圖片完成桌機與手機截圖。桌機 1440×1000 下，輪播以 3:1 顯示於「最新開獎號碼」正上方，圓點位置正常；手機 390×844 下，輪播改用 16:7 容器，左右箭頭及圓點均清楚可用，下方內容銜接正常。截圖完成後，暫時 QA 程式分支已移除，正式頁面仍只讀取後台資料。

由於 3:1 原圖在手機 16:7 容器會以置中方式裁去左右各約 12%，正式廣告建議使用 **1500×500 px**，並將文字、Logo 與人物等重要內容放在中央約 **1140×500 px** 的安全區內；左右各約 180 px 只放可裁切的背景。需要更高解析度時可使用 1800×600 px，維持相同比例與安全區原則。

## 自動化驗證

最終 TypeScript 型別檢查通過；Vitest 共 20 個測試檔、95 項測試全部通過，其中包含 HomeCarousel 與 CarouselManagement 的 jsdom React 渲染及互動測試；Vite 前端與 Node 伺服器正式建置均成功。建置僅保留既有的大型 chunk 提示，沒有編譯錯誤。
