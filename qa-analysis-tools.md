# 分析工具區 QA 紀錄

## 執行期與後台權限

2026-07-21 重啟後實際開啟首頁及 `/admin?section=members`，兩頁均完成載入；最新伺服器日誌顯示 TypeScript 0 錯誤，瀏覽器日誌只有 Vite 連線紀錄，未再出現 `LEGACY_PERMISSION_KEYS` 匯出錯誤。

後台「群組全域權限」的實際頁面只顯示 7 項功能，其中分析工具為：**分布與統計表、單雙比與和值、頭數與尾數、距今未開**；舊的統計表、和值、連莊重複與尾數均未出現。一般會員及付費會員兩欄的新名稱一致。

## 前台工具名稱

以管理員身分實際返回首頁並展開「走勢分析」，前台只顯示 **分布與統計表、單雙比與和值、頭數與尾數、距今未開** 四個工具；舊的統計表、和值、連莊重複與尾數均未出現。首頁資料與輪播同時正常載入。

## 分布與統計表

實際開啟「分布與統計表」後，頁面顯示日期區間／期數區間二擇一、管理員不限期數、起訖欄位與「包含特別號」選項。日期查詢完成後由舊到新顯示 118 期大樂透，矩陣固定列出 01～49；命中號碼以大樂透黃色圓球及白字呈現，表尾包含 01～49 的次數統計、內含數字的迷你長條圖與依少至多排序的次數排序列。

## 單雙比與和值

實際開啟「單雙比與和值」並完成相同日期查詢後，逐期顯示開獎日期、期數、6 個大樂透號碼、特別號、單雙比及和值。未勾選「包含特別號」時，特別號欄保持空白，單雙比與和值只依 6 個固定號碼計算；例如 114.07.22 的 05、17、19、26、27、39 顯示單雙比 5:1、和值 133，與人工加總一致。

## 頭數與尾數

實際開啟「頭數與尾數」並完成查詢後，大樂透逐期表格固定顯示 0～4 頭及 0～9 尾；每期各頭、各尾的計數合計均為 6。以 114.07.22 的 05、17、19、26、27、39 為例，頭數為 0頭=1、1頭=2、2頭=2、3頭=1、4頭=0；尾數為 5尾=1、6尾=1、7尾=2、9尾=2，其餘為 0，與原始號碼一致。

## 距今未開

實際開啟「距今未開」並完成查詢後，矩陣由舊到新顯示日期／期數與 01～49。命中號碼以淺藍底黑字呈現；未命中格以灰字遞增顯示，第一列已帶入查詢區間之前的累積遺漏值，例如 114.07.22 的 01 為 11、03 為 8，而非從 1 重新起算。畫面可水平捲動至末端；目前最後一次命中後的連續遺漏區段使用黃色底色，表尾顯示 01～49 的全歷史最大值。

## 期數區間模式

實際將「距今未開」由日期區間切換為期數區間後，日期欄位完全改為「起始期數」與「結束期數」文字輸入；已成功輸入起始期數 115000001，管理員不限期數提示與特別號選項仍正常保留。

已輸入結束期數 115000010 並點擊「查詢分析」；畫面進入「正在分析 Google Sheets 資料…」狀態，證明期數區間條件已成功送至分析 API。

查詢完成後畫面顯示「實際分析 10 期，共符合 10 期」，資料從 115.01.02／115000001 到 115.02.03／115000010 由舊到新排列；距今未開第一列仍承接區間前遺漏值，末端目前連續未開格以黃色標示，並保留全歷史最大值列。

## 手機 390×844 驗證

以 390×844 手機視窗擷取完整頁面：頁首、分析工具區、日期／期數切換、起訖欄位、包含特別號、查詢按鈕與「實際分析 10 期」均以單欄排列；距今未開表格可在卡片內水平捲動，畫面清楚呈現淺藍命中、黃色目前連續未開、號碼列及最大值列。截圖所用的開發 QA 分支已立即移除，正式頁面仍只讀取登入會員及真實分析 API。

移除 QA 分支後再次載入正式首頁成功，網址不含任何測試參數。重新檢查 17:53 服務重啟後的伺服器與瀏覽器日誌，均未出現 `LEGACY_PERMISSION_KEYS`、`SyntaxError`、`Error` 或 `Failed`；最新紀錄只有 Vite 連線與開發除錯器初始化。最終 28 個測試檔、123 項測試全部通過，Vite 與 Node 正式建置成功。

## 重啟後正式分析頁直接證據

17:53 服務重啟後，瀏覽器恢復為真實管理員 `terry lee` 工作階段；展開「走勢分析」可見且僅見「分布與統計表、單雙比與和值、頭數與尾數、距今未開」四個工具。點擊「分布與統計表」後，正式分析頁直接載入成功，畫面顯示 `TREND ANALYSIS`、日期區間／期數區間、起訖日期、包含特別號、查詢分析及「正在分析 Google Sheets 資料…」。本次網址不含 QA 參數，使用正式會員與正式 API 資料流。

同次正式查詢完成後顯示「管理員不限期數」與「實際分析 118 期，共符合 118 期」，並呈現 01～49 號碼矩陣、黃色命中球、次數統計及次數排序；對應的會員與分析 API 請求均回傳 HTTP 200。隨即核對 17:53 重啟後至 18:05 的伺服器與瀏覽器日誌，未出現 `LEGACY_PERMISSION_KEYS`、`SyntaxError`、`Error` 或 `Failed`，完成正式分析頁成功載入與乾淨執行期日誌的雙重佐證。
